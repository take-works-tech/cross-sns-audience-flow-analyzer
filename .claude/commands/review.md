---
description: Review the current changes against the 5 perspectives before PR.
---

Review the current diff for cross-sns-audience-flow-analyzer (delegate to the `reviewer` agent if available).

Apply EVERY perspective to EVERY changed file:
code quality / security / spec traceability / simplification / frontend.
Report by severity (Critical / High / Medium / Best Practices) with `file:line`.
Approve only when Critical = High = Medium = 0.

$ARGUMENTS
