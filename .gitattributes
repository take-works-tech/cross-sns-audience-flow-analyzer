# Normalize line endings: LF in repo, LF on checkout (cross-platform toolchain: Python hooks, sh scripts, CI)
* text=auto eol=lf

# Windows scripts keep CRLF
*.cmd text eol=crlf
*.ps1 text eol=crlf

# Binary
*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.woff2 binary
